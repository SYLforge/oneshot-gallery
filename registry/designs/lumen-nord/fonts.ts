// Self-hosted via @fontsource — no build-time Google Fonts fetch.
import "@fontsource/hahmlet";
import "@fontsource/sora";

export const hahmlet = { variable: "" };
export const sora = { variable: "" };
